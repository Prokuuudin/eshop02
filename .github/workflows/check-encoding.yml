name: Text Encoding Check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check-encoding:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - run: node scripts/check-public-text-encoding.mjs
